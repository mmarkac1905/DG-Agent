# DG AI Agent — environment template.
# Copy to `.env` (which is git-ignored) and fill in your value. Never commit a real key.

# Anthropic API key — REQUIRED for the AI pipeline:
#   Stage A scope derivation, the LLM EDA analyzers, and Stage D S2T generation.
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here
